interface:
  display_name: "Harness Flow"
  short_description: "Features, gates y vault Obsidian; PRD/SDD, Jira/Confluence."
  default_prompt: "Usa $harness-flow para gestionar el flujo de features de este proyecto."

policy:
  allow_implicit_invocation: true
